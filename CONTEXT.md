# Context

## Glossary

### File content search

Search within rendered file-like content, including file previews and changed diffs. The search behaviour includes opening controls, matching text, highlighting matches, navigating between matches, preserving search across re-renders, and scrolling the active match into view.
